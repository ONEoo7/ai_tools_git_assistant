# The TUF verifier this application is built against.
#
# Pinned in the repository rather than held in a CI variable so that changing
# it is a commit someone reviews. This wheel verifies every update the
# application will ever install; fetching whatever currently sits at a URL
# would make the release workflow the weakest link in a chain built to avoid
# exactly that.
#
# The release workflow downloads this URL, checks the digest, and refuses to
# build if they disagree.
#
# To update: publish a new wheel from the distribution platform
# (`git tag dist-client-vX.Y.Z`), then replace both lines. The digest is
# printed in that workflow's job summary and is also visible as the release
# asset's `digest` field.
#
# Provenance: this wheel carries a SLSA attestation naming
# ONEoo7/dev_tools_distribution_platform/.github/workflows/release-dist-client.yml
# Verify it with:
#   gh attestation verify <wheel> --repo ONEoo7/dev_tools_distribution_platform

url = https://github.com/ONEoo7/dev_tools_distribution_platform/releases/download/dist-client-v0.1.0/dist_client-0.1.0-py3-none-win_amd64.whl
sha256 = f34c4abbfed71e12b76f0c02f50cc0019ff70d8f002927f43f7a8bf66116ca17
